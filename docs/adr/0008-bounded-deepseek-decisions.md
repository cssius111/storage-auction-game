# ADR 0008：DeepSeek 仅做受约束决策

- 状态：Accepted
- 日期：2026-07-23

## 背景

直接让模型控制规则会导致不可复现、越权和资金错误。

## 决策

DeepSeek 只从服务端提供的合法动作 ID 中选择，并可生成短台词。真相、观察、价格映射、状态转换和账本由确定性代码控制。

## 影响

响应必须做 schema 校验；非法或失败响应使用规则 AI。每仓库最多 18 次调用。

